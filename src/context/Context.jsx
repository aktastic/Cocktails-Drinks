import { createContext } from "react";

export const UserSearchInputContext = createContext('');
export const SearchFetchContext = createContext([]);
export const FetchButtonContext = createContext(false);